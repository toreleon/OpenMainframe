# OpenMainframe Kubernetes Deployment
# Production-ready deployment manifest
apiVersion: apps/v1
kind: Deployment
metadata:
  name: open-mainframe
  labels:
    app: open-mainframe
    version: v1.3.0
spec:
  replicas: 3
  selector:
    matchLabels:
      app: open-mainframe
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: open-mainframe
        version: v1.3.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: open-mainframe
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: open-mainframe
          image: ghcr.io/toreleon/OpenMainframe:v1.3.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          env:
            - name: OPEN_MAINFRAME_LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: open-mainframe-config
                  key: log_level
            - name: OPEN_MAINFRAME_LOG_FORMAT
              valueFrom:
                configMapKeyRef:
                  name: open-mainframe-config
                  key: log_format
            - name: OPEN_MAINFRAME_DB_URL
              valueFrom:
                secretKeyRef:
                  name: open-mainframe-secrets
                  key: database_url
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: open-mainframe-config
                  key: otel_endpoint
                  optional: true
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: config
              mountPath: /etc/open-mainframe
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: config
          configMap:
            name: open-mainframe-config
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: open-mainframe
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: open-mainframe
  labels:
    app: open-mainframe
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
  selector:
    app: open-mainframe
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: open-mainframe
  labels:
    app: open-mainframe
