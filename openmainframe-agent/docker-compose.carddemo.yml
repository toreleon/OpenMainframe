services:
  carddemo-bridge:
    build:
      context: ..
      dockerfile: openmainframe-agent/Dockerfile.carddemo
    environment:
      - BRIDGE_SERVER_URL=ws://host.docker.internal:8123/ws/bridge
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: on-failure
