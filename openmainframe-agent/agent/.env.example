# LLM Provider — set ONE of these
OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# LLM Configuration
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4.1-mini

# OpenMainframe binary path (relative to repo root or absolute)
OPEN_MAINFRAME_BIN=../../target/release/open-mainframe

# Workspace root (restricts file access to this directory)
WORKSPACE_ROOT=../../

# Server port
PORT=8123

# Checkpointer backend: "memory" (default) or "postgres"
# CHECKPOINTER=memory

# PostgreSQL persistence (required when CHECKPOINTER=postgres)
# Install with: pip install 'openmainframe-agent[postgres]'
# POSTGRES_URI=postgresql://user:pass@localhost:5432/openmainframe
